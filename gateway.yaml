server:
  port: 8080
spring:
  cloud: 
   gateway: 
    routes:
      - id: catalogevent
        uri: lb://evento/
        predicates:
          - Path=/evento/**
      - id: databaseusers
        uri: lb://user/
        predicates:
          - Path=/user/**
